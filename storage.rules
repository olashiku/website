rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /projects/{allPaths=**} {
      // Allow public read access to all project media
      allow read: if true;
      
      // Allow upload and delete access only to authenticated users
      allow write: if request.auth != null;
    }
  }
}
